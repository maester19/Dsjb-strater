{% extends 'base/base.html.twig' %}

{% block title %}Edit User{% endblock %}
{% block stylesheets %}
<style>
    body{
        background: url("{{ asset('img/edit2.jpeg') }}");
        background-repeat: no-repeat;
        background-size:cover;
    }
    .block{
        background:rgba(59, 78, 78, 0.8);
        width:40%;
        color:white;
        border-radius:10px;
        padding:10px;
    }
</style>
{% endblock %}

{% block body %}
    <div class="block m-5">
        <h1>Edit User: <a href="{{ path('admin.user.show', {'id': user.id}) }} ">{{ user.nom }} </a></h1>

        {{ include('admin/user/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('admin.user.index') }}" class="btn btn-secondary" style="display: inline-block"> < < back to list</a>
        {{ include('admin/user/_delete_form.html.twig') }}
    </div>
{% endblock %}
